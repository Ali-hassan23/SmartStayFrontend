import React from 'react'

const StaffDisplay = ({ staff }) => {
    if (!staff) {
        return <div>Loading...</div>;
      }

  return (
    <div>
      
    </div>
  )
}

export default StaffDisplay
